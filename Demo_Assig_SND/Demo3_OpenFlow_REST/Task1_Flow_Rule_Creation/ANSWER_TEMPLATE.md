# Answer Template — Task 1

## Task 1.1 — Flow Rules on the Predefined Topology

### Rule 1 — Block ICMP from Blue to Red

Command used:

```text
```

Rule details and explanation:

```text
```

Validation evidence:

```text
Flow output:
Traffic result:
```

### Rule 2 — Block traffic to Red

Command used:

```text
```

Rule details and explanation:

```text
```

Validation evidence:

```text
Flow output:
Traffic result:
```

### Rule 3 — Allow total access to Red from Green and Blue

Command used:

```text
```

Rule details and explanation:

```text
```

Validation evidence:

```text
Flow output:
Traffic result:
```

### Rule 4 — Must the previous blocking rule be deleted?

```text
Explain whether the second rule must be deleted before the third rule takes effect, and why.
```

### Rule 5 — Allow only HTTP and HTTPS to Red

Command used:

```text
```

Rule details and explanation:

```text
```

Validation evidence:

```text
Flow output:
Traffic result:
```

## Task 1.2 — Pseudo Network Slice

File created for the sliced topology:

```text
```

How to run this file:

```text
```

First isolation approach:

```text
```

Validation evidence:

```text
Topology output:
Isolation result:
```

Second isolation approach:

```text
```

Validation evidence:

```text
Topology output:
Isolation result:
```

## Problems and Fixes

```text
Issue:
Root cause:
Fix:
```

## Final Status

- [ ] Completed Task 1.1
- [ ] Completed Task 1.2
- [ ] Included validation evidence
- [ ] Cleaned up environment
